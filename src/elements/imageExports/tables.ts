import type { GuideImage } from "../types/GuideImage"

export const tableSelectionLocation: GuideImage = {
  src: '/images/tables/table_selection.png',
  alt: 'Where to select the table',
  caption: 'Where to select the table'
}